name: first_one
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: Lint
      run: eslint --ext .jsx --fix src
    - name: Test
      run: vitest run
    - name: Build
      run: vite build
    - name:  Message
      run: echo "Output message"
      
